# =============================================================================
# pair_bot/config.py
# 페어 트레이딩 봇 — 모든 설정값 관리 파일
# 여기 값만 바꾸면 봇 동작이 바뀝니다.
# =============================================================================

# ── API 키 ─────────────────────────────────────────────────────────────────
API_KEY    = "YOUR_BINANCE_API_KEY"
API_SECRET = "YOUR_BINANCE_API_SECRET"

# ── 페이퍼 트레이딩 모드 ────────────────────────────────────────────────────
# True : 실제 주문 없이 가상 시뮬레이션
# False: 실제 바이낸스 선물 주문 집행
IS_PAPER_TRADING = True

# ── 거래 대상 심볼 ──────────────────────────────────────────────────────────
SYMBOL_A = "SOL/USDT:USDT"   # 바이낸스 선물 표기법
SYMBOL_B = "AVAX/USDT:USDT"

# ── 레버리지 ────────────────────────────────────────────────────────────────
LEVERAGE = 3   # 3~5 권장

# ── 주문당 투입 USDT (증거금 기준) ──────────────────────────────────────────
TRADE_USDT = 50.0   # 한 쌍 진입 시 각 레그당 달러 금액

# ── 스프레드/Z-Score 엔진 설정 ───────────────────────────────────────────────
# 가격 비율(ratio = price_A / price_B) 계산에 사용할 데이터 포인트 수
# 1초 폴링 기준: 3600 = 약 1시간
RATIO_WINDOW = 3600

# 진입 임계치 (비율의 %)
ENTRY_THRESHOLD_PCT = 0.3   # 0.3% 이상 괴리 → 진입

# 청산 임계치 (비율의 %)
EXIT_THRESHOLD_PCT  = 0.05  # 0.05% 이내로 회귀 → 익절

# 손절 임계치 (비율의 %)
STOP_LOSS_PCT       = 1.5   # 1.5% 이상 확대 → 즉시 손절

# ── 폴링 간격 (초) ──────────────────────────────────────────────────────────
POLL_INTERVAL_SEC = 1.0

# ── 주문 재시도 설정 ────────────────────────────────────────────────────────
ORDER_RETRY_COUNT   = 3
ORDER_RETRY_WAIT    = 2.0   # 초

# ── 로그 파일 경로 ──────────────────────────────────────────────────────────
LOG_FILE = "bot.log"
